<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shams.moviestageone">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".movie.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".movie.MovieDetailsActivity"
            android:label="@string/movie_details"
            android:parentActivityName=".movie.main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".movie.main.MainActivity" />
        </activity>
        <activity
            android:name=".setting.preference.SettingActivity"
            android:label="@string/filter"
            android:parentActivityName=".movie.main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".movie.main.MainActivity" />
        </activity>
        <activity
            android:name=".movie.favourite.FavouriteActivity"
            android:label="@string/favourite_movie"
            android:parentActivityName=".movie.main.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".movie.main.MainActivity" />
        </activity>

        <provider
            android:authorities="com.example.shams.moviestageone"
            android:name=".database.FMProvider"
            android:exported="false" />

    </application>

</manifest>